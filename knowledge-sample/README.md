# 脱敏知识包示例

此目录只包含合成规则和接口示例，用于验证索引、SKILL 路由、引用与冲突体验，不代表项目私有知识正文，也不能作为真实营销结论。

本地完整 Dogfood 应通过 `KNOWLEDGE_PACK_PATH` 挂载私有知识包。要单独验证公开示例，可临时设置：

```bash
KNOWLEDGE_PACK_PATH=./knowledge-sample
```

示例不包含真实客户、企业事实、价格、承诺、会话、书籍或外部绝对路径。
